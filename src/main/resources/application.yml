spring:
  data:
    mongodb:
      uri: mongodb://test:test@localhost:27017/pay_api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/pay_api
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: create